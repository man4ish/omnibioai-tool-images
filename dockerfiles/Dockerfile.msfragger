FROM ubuntu:24.04
RUN apt-get update && apt-get install -y \
    default-jre \
    wget \
    python3 \
    python3-pip \
    && pip3 install --break-system-packages pyteomics
# MSFragger requires academic license download
# Download from https://msfragger.nesvilab.org/ and place at /opt/MSFragger.jar
COPY MSFragger.jar /opt/MSFragger.jar
RUN java -jar /opt/MSFragger.jar --version 2>&1 || true
CMD ["java", "-jar", "/opt/MSFragger.jar", "--version"]
