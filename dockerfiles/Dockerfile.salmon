FROM ubuntu:24.04
RUN apt-get update && apt-get install -y \
    libgomp1 \
    zlib1g \
    libbz2-1.0 \
    liblzma5 \
    libstdc++6
COPY salmon /usr/local/bin/salmon
RUN chmod +x /usr/local/bin/salmon
CMD ["salmon", "--version"]
